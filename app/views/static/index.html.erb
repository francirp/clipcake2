
<div class="hero-unit">
  <div class="media">
    <a class="pull-left">
      <%= image_tag "book.png", :class => "media-object pull-left home_picture" %>
    </a>
    <div class="media-body">
        <h2 class="media-heading home_callout">Community Created Photobooks</h2>
        <p class="home_callout">Create a powerful gift for someone you care about - with the people that they care about.</p>
        <div class="button_callout"><a href="<%= if current_user.present? then new_book_url else '/auth/facebook' end %>"><button class="btn btn-success btn-large">Get Started Now</button></a></div>
    </div>
  </div>
</div>

<div class="row-fluid">
  <ul class="thumbnails">
    <li class="span4">
      <div class="thumbnail">
        <%= image_tag "book.png" %>
        <div class="caption">
          <h3>Birthday Photobook</h3>
          <p>Create a photobook for a friend or family member's birthday.</p>
          <div class="button_callout"><a href="<%= if current_user.present? then new_book_url else '/auth/facebook' end %>"><button class="btn btn-success btn-large">Get Started Now</button></a></div>
        </div>
      </div>
    </li>

    <li class="span4">
      <div class="thumbnail">
        <%= image_tag "book.png" %>
        <div class="caption">
          <h3>Wedding Photobook</h3>
          <p>Create a photobook for a friend or family member's wedding.</p>
          <div class="button_callout"><a href="<%= if current_user.present? then new_book_url else '/auth/facebook' end %>"><button class="btn btn-success btn-large">Get Started Now</button></a></div>
        </div>
      </div>
    </li>

    <li class="span4">
      <div class="thumbnail">
        <%= image_tag "book.png" %>
        <div class="caption">
          <h3>Graduation Photobook</h3>
          <p>Create a photobook for a friend or family member's graduation.</p>
          <div class="button_callout"><a href="<%= if current_user.present? then new_book_url else '/auth/facebook' end %>"><button class="btn btn-success btn-large">Get Started Now</button></a></div>
        </div>
      </div>
    </li>
